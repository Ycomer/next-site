import Layout from '../../../../components/learn/Layout';
import Logo from '../../../../components/logo';
import Link from 'next/link';

export const meta = {
  title: 'Create and Deploy a Next.js App',
  courseId: 'basics',
  lessonId: 'create-and-deploy'
};

<p>
  <Logo size={80} />
</p>

**Next.js** is the React framework for building JavaScript applications. It’s used and loved by <Link href="/showcase"><a>the world’s leading companies</a></Link>.

In this tutorial, you’ll learn Next.js basics by creating a **blog app**. We’ll cover the following features of Next.js:

- (TODO)
- (TODO)
- (TODO)

### Final Result

You can take a look at the final result here: [TODO](https://learn-next-chibicode.now.sh/).

### What You’ll Learn in This Lesson

- TODO

> This tutorial assumes basic knowledge of JavaScript and React. If you’ve never written a React component, you should go through [the official React tutorial](https://reactjs.org/tutorial/tutorial.html) first.

Let’s get started!

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
