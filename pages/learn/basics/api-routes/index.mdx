import Layout from '../../../../components/learn/Layout';
import Link from 'next/link';

export const meta = {
  title: 'API Routes',
  courseId: 'basics',
  lessonId: 'api-routes'
};

Next.js has support for API Routes, which lets you easily create an API endpoint as a Node.js function. Although it’s not necessary for our blog app, we’ll briefly talk about how to use it in this final lesson. You’re almost done!

### What You’ll Learn in This Lesson

In this lesson, you’ll learn:

- How to create an API Route.
- About some useful information on dynamic routes.
- What to do and learn next after you’re done with this tutorial.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
