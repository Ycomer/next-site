import Layout from '../../../../components/learn/Layout';

export const meta = {
  title: 'Data Fetching',
  courseId: 'basics',
  lessonId: 'data-fetching',
  stepId: 'setup',
  subtitle: 'Setup'
};

### Download Starter Code (Optional)

**If you’re continuing from the previous lesson,** you can skip the following.

Otherwise, you can download, install, and run the starter code for this lesson below. Again, this is NOT necessary if you’ve just finished the previous lesson.

```bash
# If you prefer Yarn, you can use `yarn create` instead of `npm init`
npm init next-app nextjs-blog --example "https://github.com/zeit/next-learn-starter/tree/master/data-fetching-starter"
cd nextjs-blog
# If you prefer Yarn, you can use `yarn dev` instead
npm run dev
```

### Create a Branch (Optional)

If you’d like to follow the **DPS** workflow (**D**evelop, **P**review, **S**hip), you can:

- Create a new branch (e.g. `data-fetching`), and
- Open a pull request after making a change to see preview deployments.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
