import Layout from '../../../../components/learn/Layout';
import Image from '../../../../components/image';
import PrerenderIndicator from '../../../../components/learn/prerender-indicator';
import Link from 'next/link';

export const meta = {
  title: 'Getting Started',
  courseId: 'basics',
  lessonId: 'getting-started',
  stepId: 'welcome-to-nextjs'
};

## Welcome to Next.js

You should see a page like this when you access [http://localhost:3000](http://localhost:3000). This is the starter template page which shows some helpful information about Next.js.

<Image
  shadow
  alt="Welcome to Next.js"
  oversize={false}
  src="/static/images/learn/getting-started/welcome-to-nextjs.png"
  width={1576 / 2}
  height={1284 / 2}
/>

> **Help is available**: If you see an error or get stuck, you can always ask a question on [GitHub Discussions](https://github.com/zeit/next.js/discussions).

### Deploy to Production

Now that your Next.js app is up and running locally, let’s **deploy** it to production.

You might be wondering: “Deploy to production? We haven’t even made any changes yet!” But you’ll have to deploy your app to production at some point anyway, so why not learn how to do so now?

Don’t worry — the deployment will be quick and simple. Let’s do it on the next page.

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
